---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Descripción de los filtros de acción (C-) Microsoft Docs
author: rick-anderson
description: El objetivo de este tutorial es explicar los filtros de acción. Un filtro de acción es un atributo que se puede aplicar a una acción de controlador o a un controlador completo...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: 75ba7b1dce280a45cd092de97c464eade5f49838
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542188"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="f8955-104">Descripción de los filtros de acción (C#)</span><span class="sxs-lookup"><span data-stu-id="f8955-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="f8955-105">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="f8955-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="f8955-106">Descargar PDF</span><span class="sxs-lookup"><span data-stu-id="f8955-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="f8955-107">El objetivo de este tutorial es explicar los filtros de acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="f8955-108">Un filtro de acción es un atributo que se puede aplicar a una acción de controlador (o a un controlador completo) que modifica la forma en que se ejecuta la acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="f8955-109">Comprender los filtros de acción</span><span class="sxs-lookup"><span data-stu-id="f8955-109">Understanding Action Filters</span></span>

<span data-ttu-id="f8955-110">El objetivo de este tutorial es explicar los filtros de acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="f8955-111">Un filtro de acción es un atributo que se puede aplicar a una acción de controlador (o a un controlador completo) que modifica la forma en que se ejecuta la acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="f8955-112">El marco de ASP.NET MVC incluye varios filtros de acción:</span><span class="sxs-lookup"><span data-stu-id="f8955-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="f8955-113">OutputCache: este filtro de acción almacena en caché la salida de una acción de controlador durante un período de tiempo especificado.</span><span class="sxs-lookup"><span data-stu-id="f8955-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="f8955-114">HandleError: este filtro de acción controla los errores generados cuando se ejecuta una acción del controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="f8955-115">Autorizar: este filtro de acción le permite restringir el acceso a un usuario o rol determinado.</span><span class="sxs-lookup"><span data-stu-id="f8955-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="f8955-116">También puede crear sus propios filtros de acción personalizados.</span><span class="sxs-lookup"><span data-stu-id="f8955-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="f8955-117">Por ejemplo, es posible que desee crear un filtro de acción personalizado para implementar un sistema de autenticación personalizado.</span><span class="sxs-lookup"><span data-stu-id="f8955-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="f8955-118">O bien, es posible que desee crear un filtro de acción que modifique los datos de vista devueltos por una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="f8955-119">En este tutorial, aprenderá a crear un filtro de acción desde cero.</span><span class="sxs-lookup"><span data-stu-id="f8955-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="f8955-120">Creamos un filtro de acción Registro que registra diferentes etapas del procesamiento de una acción en la ventana Salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f8955-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="f8955-121">Uso de un filtro de acción</span><span class="sxs-lookup"><span data-stu-id="f8955-121">Using an Action Filter</span></span>

<span data-ttu-id="f8955-122">Un filtro de acción es un atributo.</span><span class="sxs-lookup"><span data-stu-id="f8955-122">An action filter is an attribute.</span></span> <span data-ttu-id="f8955-123">Puede aplicar la mayoría de los filtros de acción a una acción de controlador individual o a un controlador completo.</span><span class="sxs-lookup"><span data-stu-id="f8955-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="f8955-124">Por ejemplo, el controlador de datos en `Index()` el listado 1 expone una acción denominada que devuelve la hora actual.</span><span class="sxs-lookup"><span data-stu-id="f8955-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="f8955-125">Esta acción está `OutputCache` decorada con el filtro de acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="f8955-126">Este filtro hace que el valor devuelto por la acción se almacene en caché durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="f8955-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="f8955-127">**Listado 1 –`Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="f8955-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="f8955-128">Si invoca repetidamente `Index()` la acción introduciendo la dirección URL /Data/Index en la barra de direcciones de su navegador y pulsando el botón Actualizar varias veces, verá la misma hora durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="f8955-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="f8955-129">La salida `Index()` de la acción se almacena en caché durante 10 segundos (véase el cuadro 1).</span><span class="sxs-lookup"><span data-stu-id="f8955-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="f8955-130">[![Tiempo en caché](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="f8955-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="f8955-131">**Figura 01**: Tiempo en caché[(Haga clic para ver la imagen de tamaño completo)](understanding-action-filters-cs/_static/image3.png)</span><span class="sxs-lookup"><span data-stu-id="f8955-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="f8955-132">En el listado 1, se `OutputCache` aplica un único `Index()` filtro de acción (el filtro de acción) al método.</span><span class="sxs-lookup"><span data-stu-id="f8955-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="f8955-133">Si lo necesita, puede aplicar varios filtros de acción a la misma acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="f8955-134">Por ejemplo, es posible que `OutputCache` `HandleError` desee aplicar los filtros y los filtros de acción a la misma acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="f8955-135">En el listado `OutputCache` 1, el `Index()` filtro de acción se aplica a la acción.</span><span class="sxs-lookup"><span data-stu-id="f8955-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="f8955-136">También podría aplicar este `DataController` atributo a la propia clase.</span><span class="sxs-lookup"><span data-stu-id="f8955-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="f8955-137">En ese caso, el resultado devuelto por cualquier acción expuesta por el controlador se almacenaría en caché durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="f8955-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="f8955-138">Los diferentes tipos de filtros</span><span class="sxs-lookup"><span data-stu-id="f8955-138">The Different Types of Filters</span></span>

<span data-ttu-id="f8955-139">El marco de ASP.NET MVC admite cuatro tipos diferentes de filtros:</span><span class="sxs-lookup"><span data-stu-id="f8955-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="f8955-140">Filtros de autorización: implementa el `IAuthorizationFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="f8955-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="f8955-141">Filtros de acción: implementa el `IActionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="f8955-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="f8955-142">Filtros de resultados: implementa el `IResultFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="f8955-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="f8955-143">Filtros de excepción: implementa el `IExceptionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="f8955-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="f8955-144">Los filtros se ejecutan en el orden indicado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="f8955-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="f8955-145">Por ejemplo, los filtros de autorización siempre se ejecutan antes de que los filtros de acción y los filtros de excepción siempre se ejecuten después de cualquier otro tipo de filtro.</span><span class="sxs-lookup"><span data-stu-id="f8955-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="f8955-146">Los filtros de autorización se utilizan para implementar la autenticación y la autorización para las acciones del controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="f8955-147">Por ejemplo, el filtro Autorizar es un ejemplo de un filtro Autorización.</span><span class="sxs-lookup"><span data-stu-id="f8955-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="f8955-148">Los filtros de acción contienen lógica que se ejecuta antes y después de que se ejecute una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="f8955-149">Puede usar un filtro de acción, por ejemplo, para modificar los datos de vista que devuelve una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="f8955-150">Los filtros de resultados contienen lógica que se ejecuta antes y después de ejecutar un resultado de vista.</span><span class="sxs-lookup"><span data-stu-id="f8955-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="f8955-151">Por ejemplo, es posible que desee modificar un resultado de vista justo antes de que la vista se represente en el explorador.</span><span class="sxs-lookup"><span data-stu-id="f8955-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="f8955-152">Los filtros de excepción son el último tipo de filtro que se ejecuta.</span><span class="sxs-lookup"><span data-stu-id="f8955-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="f8955-153">Puede usar un filtro de excepciones para controlar los errores generados por las acciones del controlador o los resultados de la acción del controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="f8955-154">También puede usar filtros de excepción para registrar errores.</span><span class="sxs-lookup"><span data-stu-id="f8955-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="f8955-155">Cada tipo diferente de filtro se ejecuta en un orden determinado.</span><span class="sxs-lookup"><span data-stu-id="f8955-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="f8955-156">Si desea controlar el orden en el que se ejecutan los filtros del mismo tipo, puede establecer la propiedad Order de un filtro.</span><span class="sxs-lookup"><span data-stu-id="f8955-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="f8955-157">La clase base para todos `System.Web.Mvc.FilterAttribute` los filtros de acción es la clase.</span><span class="sxs-lookup"><span data-stu-id="f8955-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="f8955-158">Si desea implementar un tipo determinado de filtro, debe crear una clase que herede de la `IAuthorizationFilter`clase `IActionFilter` `IResultFilter`Filter `IExceptionFilter` base e implemente una o varias de las interfaces , , , o .</span><span class="sxs-lookup"><span data-stu-id="f8955-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="f8955-159">La clase Base ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="f8955-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="f8955-160">Para facilitar la implementación de un filtro de acción personalizado, `ActionFilterAttribute` el marco de ASP.NET MVC incluye una clase base.</span><span class="sxs-lookup"><span data-stu-id="f8955-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="f8955-161">Esta clase implementa `IActionFilter` las `IResultFilter` interfaces y hereda de la `Filter` clase.</span><span class="sxs-lookup"><span data-stu-id="f8955-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="f8955-162">La terminología aquí no es del todo consistente.</span><span class="sxs-lookup"><span data-stu-id="f8955-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="f8955-163">Técnicamente, una clase que hereda de la clase ActionFilterAttribute es un filtro de acción y un filtro de resultados.</span><span class="sxs-lookup"><span data-stu-id="f8955-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="f8955-164">Sin embargo, en el sentido suelto, la palabra filtro de acción se usa para hacer referencia a cualquier tipo de filtro en el marco de ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="f8955-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="f8955-165">La `ActionFilterAttribute` clase base tiene los siguientes métodos que puede invalidar:</span><span class="sxs-lookup"><span data-stu-id="f8955-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="f8955-166">OnActionExecuting: se llama a este método antes de ejecutar una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="f8955-167">OnActionExecuted: se llama a este método después de ejecutar una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="f8955-168">OnResultExecuting: se llama a este método antes de ejecutar un resultado de acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="f8955-169">OnResultExecuted: se llama a este método después de ejecutar un resultado de acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="f8955-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="f8955-170">En la siguiente sección, veremos cómo puede implementar cada uno de estos métodos diferentes.</span><span class="sxs-lookup"><span data-stu-id="f8955-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="f8955-171">Creación de un filtro de acción de registro</span><span class="sxs-lookup"><span data-stu-id="f8955-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="f8955-172">Para ilustrar cómo puede crear un filtro de acción personalizado, crearemos un filtro de acción personalizado que registra las etapas de procesamiento de una acción de controlador en la ventana Salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f8955-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="f8955-173">Nuestro `LogActionFilter` está contenido en el listado 2.</span><span class="sxs-lookup"><span data-stu-id="f8955-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="f8955-174">**Listado 2 –`ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="f8955-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="f8955-175">En el listado `OnActionExecuting()` `OnActionExecuted()`2, `OnResultExecuted()` los métodos `Log()` , , `OnResultExecuting()`, y todos llaman al método.</span><span class="sxs-lookup"><span data-stu-id="f8955-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="f8955-176">El nombre del método y los datos `Log()` de ruta actuales se pasan al método.</span><span class="sxs-lookup"><span data-stu-id="f8955-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="f8955-177">El `Log()` método escribe un mensaje en la ventana Salida de Visual Studio (consulte la figura 2).</span><span class="sxs-lookup"><span data-stu-id="f8955-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="f8955-178">[![Escribir en la ventana Salida de Visual Studio](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="f8955-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="f8955-179">**Figura 02**: Escritura en la ventana Salida de Visual Studio (Haga clic para ver la[imagen de tamaño completo)](understanding-action-filters-cs/_static/image6.png)</span><span class="sxs-lookup"><span data-stu-id="f8955-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="f8955-180">El controlador de inicio en el listado 3 ilustra cómo puede aplicar el filtro de acción de registro a una clase de controlador completa.</span><span class="sxs-lookup"><span data-stu-id="f8955-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="f8955-181">Siempre que se invoca cualquiera de las acciones `Index()` expuestas `About()` por el controlador Home, ya sea el método o el método, las etapas de procesamiento de la acción se registran en la ventana de salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f8955-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="f8955-182">**Listado 3 –`Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="f8955-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="f8955-183">Resumen</span><span class="sxs-lookup"><span data-stu-id="f8955-183">Summary</span></span>

<span data-ttu-id="f8955-184">En este tutorial, se le presentaron los filtros de acción de ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="f8955-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="f8955-185">Ha aprendido acerca de los cuatro tipos diferentes de filtros: filtros de autorización, filtros de acción, filtros de resultados y filtros de excepción.</span><span class="sxs-lookup"><span data-stu-id="f8955-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="f8955-186">También aprendiste `ActionFilterAttribute` acerca de la clase base.</span><span class="sxs-lookup"><span data-stu-id="f8955-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="f8955-187">Por último, aprendió a implementar un filtro de acción simple.</span><span class="sxs-lookup"><span data-stu-id="f8955-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="f8955-188">Hemos creado un filtro de acción de registro que registra las fases de procesamiento de una acción de controlador en la ventana Salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f8955-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f8955-189">[Anterior](asp-net-mvc-routing-overview-cs.md)
> [Siguiente](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="f8955-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
