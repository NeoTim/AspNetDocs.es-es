---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Visualización de datos en un gráfico con ASP.NET páginas web (Razor) Microsoft Docs
author: rick-anderson
description: En este capítulo se explica cómo mostrar datos en un gráfico. En los capítulos anteriores, aprendió a mostrar los datos manualmente y en una cuadrícula. Este capítulo explica...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542890"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="9bfe0-105">Visualización de datos en un gráfico con páginas Web ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="9bfe0-106">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="9bfe0-107">En este artículo se explica cómo usar un gráfico para mostrar datos `Chart` en un sitio web de ASP.NET Web Pages (Razor) mediante la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="9bfe0-108">**Lo que aprenderás**:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="9bfe0-109">Cómo mostrar datos en un gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="9bfe0-110">Cómo aplicar estilo a los gráficos utilizando temas integrados.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="9bfe0-111">Cómo guardar gráficos y cómo almacenarlos en caché para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="9bfe0-112">Estas son las características de programación ASP.NET introducidas en el artículo:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="9bfe0-113">El `Chart` ayudante.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="9bfe0-114">La información de este artículo se aplica a ASP.NET páginas Web 1.0 y páginas Web 2.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="9bfe0-115">The Chart Helper</span><span class="sxs-lookup"><span data-stu-id="9bfe0-115">The Chart Helper</span></span>

<span data-ttu-id="9bfe0-116">Cuando desee mostrar los datos en forma `Chart` gráfica, puede utilizar la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="9bfe0-117">La `Chart` aplicación auxiliar puede representar una imagen que muestra datos en una variedad de tipos de gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="9bfe0-118">Es compatible con muchas opciones para formatear y etiquetar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="9bfe0-119">La `Chart` aplicación auxiliar puede representar más de 30 tipos de gráficos, incluidos todos los tipos de gráficos con los que podría estar familiarizado desde Microsoft Excel u otras herramientas &#8212; gráficos de área, gráficos de barras, gráficos de columnas, gráficos de líneas y gráficos circulares, junto con gráficos más especializados como gráficos de acciones.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="9bfe0-120">Descripción del **gráfico de área:** ![Imagen del tipo de gráfico de área](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="9bfe0-121">Descripción del **gráfico** ![de barras: Imagen del tipo de gráfico de barras](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="9bfe0-122">Descripción **del gráfico** ![de columnas: Imagen del tipo de gráfico de columnas](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="9bfe0-123">Descripción del **gráfico** ![de líneas: Imagen del tipo de gráfico de líneas](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="9bfe0-124">Descripción **del gráfico** ![circular: Imagen del tipo de gráfico circular](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="9bfe0-125">**Descripción del gráfico** ![de acciones: Imagen del tipo de gráfico de acciones](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="9bfe0-126">Elemento de gráfico</span><span class="sxs-lookup"><span data-stu-id="9bfe0-126">Chart Elements</span></span>

<span data-ttu-id="9bfe0-127">Los gráficos muestran datos y elementos adicionales como leyendas, ejes, series, etc.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="9bfe0-128">La siguiente imagen muestra muchos de los elementos `Chart` del gráfico que puede personalizar cuando se utiliza la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="9bfe0-129">En este artículo se muestra cómo establecer algunos (no todos) de estos elementos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descripción: Imagen que muestra los elementos del gráfico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="9bfe0-131">Creación de un gráfico a partir de datos</span><span class="sxs-lookup"><span data-stu-id="9bfe0-131">Creating a Chart from Data</span></span>

<span data-ttu-id="9bfe0-132">Los datos que se muestran en un gráfico pueden ser de una matriz, de los resultados devueltos desde una base de datos o de datos que se encuentra en un archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="9bfe0-133">Uso de una matriz</span><span class="sxs-lookup"><span data-stu-id="9bfe0-133">Using an Array</span></span>

<span data-ttu-id="9bfe0-134">Como se explica en [Introducción a ASP.NET programación de páginas Web mediante la sintaxis](https://go.microsoft.com/fwlink/?LinkId=202890)de Razor , una matriz le permite almacenar una colección de elementos similares en una sola variable.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="9bfe0-135">Puede utilizar matrices para contener los datos que desea incluir en el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="9bfe0-136">Este procedimiento muestra cómo puede crear un gráfico a partir de datos en matrices, utilizando el tipo de gráfico predeterminado.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="9bfe0-137">También muestra cómo mostrar el gráfico dentro de la página.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="9bfe0-138">Cree un nuevo archivo denominado *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="9bfe0-139">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="9bfe0-140">El código crea primero un nuevo gráfico y establece su ancho y alto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="9bfe0-141">El título del gráfico `AddTitle` se especifica mediante el método.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="9bfe0-142">Para agregar datos, `AddSeries` utilice el método.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="9bfe0-143">En este ejemplo, `name`se `xValue`utilizan `yValues` los `AddSeries` parámetros , , y del método.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="9bfe0-144">El `name` parámetro se muestra en la leyenda del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="9bfe0-145">El `xValue` parámetro contiene una matriz de datos que se muestra a lo largo del eje horizontal del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="9bfe0-146">El `yValues` parámetro contiene una matriz de datos que se utiliza para trazar los puntos verticales del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="9bfe0-147">El `Write` método representa realmente el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="9bfe0-148">En este caso, dado que no especificó un tipo de gráfico, la `Chart` aplicación auxiliar representa su gráfico predeterminado, que es un gráfico de columnas.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="9bfe0-149">Ejecute la página en el explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-149">Run the page in the browser.</span></span> <span data-ttu-id="9bfe0-150">El navegador muestra el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="9bfe0-151">Uso de una consulta de base de datos para datos de gráficos</span><span class="sxs-lookup"><span data-stu-id="9bfe0-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="9bfe0-152">Si la información que desea trazar está en una base de datos, puede ejecutar una consulta de base de datos y, a continuación, usar datos de los resultados para crear el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="9bfe0-153">Este procedimiento muestra cómo leer y mostrar los datos de la base de datos creada en el artículo [Introducción al trabajo con una base](https://go.microsoft.com/fwlink/?LinkId=202893)de datos en ASP.NET sitios de páginas Web .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="9bfe0-154">Agregue una carpeta Datos de la *aplicación\_* a la raíz del sitio web si la carpeta aún no existe.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="9bfe0-155">En la carpeta *\_Datos* de la aplicación, agregue el archivo de base de datos denominado *SmallBakery.sdf* que se describe en [Introducción al trabajo con una base](https://go.microsoft.com/fwlink/?LinkId=202893)de datos en ASP.NET sitios de páginas web .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="9bfe0-156">Cree un nuevo archivo denominado *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="9bfe0-157">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="9bfe0-158">El código abre primero la base de datos `db`SmallBakery y la asigna a una variable denominada .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="9bfe0-159">Esta variable representa `Database` un objeto que se puede utilizar para leer y escribir en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="9bfe0-160">A continuación, el código ejecuta una consulta SQL para obtener el nombre y el precio de cada producto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="9bfe0-161">El código crea un nuevo gráfico y le pasa la `DataBindTable` consulta de base de datos llamando al método del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="9bfe0-162">Este método toma dos `dataSource` parámetros: el parámetro es `xField` para los datos de la consulta y el parámetro le permite establecer qué columna de datos se utiliza para el eje X del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="9bfe0-163">Como alternativa al `DataBindTable` uso del método, `AddSeries` puede `Chart` usar el método de la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="9bfe0-164">El `AddSeries` método le `xValue` permite `yValues` establecer los parámetros y.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="9bfe0-165">Por ejemplo, en `DataBindTable` lugar de usar el método de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="9bfe0-166">Puede utilizar `AddSeries` el método de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="9bfe0-167">Ambos representan los mismos resultados.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-167">Both render the same results.</span></span> <span data-ttu-id="9bfe0-168">El `AddSeries` método es más flexible porque puede especificar el tipo `DataBindTable` de gráfico y los datos de forma más explícita, pero el método es más fácil de usar si no necesita la flexibilidad adicional.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="9bfe0-169">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="9bfe0-170">Usar datos XML</span><span class="sxs-lookup"><span data-stu-id="9bfe0-170">Using XML Data</span></span>

<span data-ttu-id="9bfe0-171">La tercera opción para el gráfico es utilizar un archivo XML como datos para el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="9bfe0-172">Esto requiere que el archivo XML también tenga un archivo de esquema (archivo *.xsd)* que describa la estructura XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="9bfe0-173">Este procedimiento muestra cómo leer datos de un archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="9bfe0-174">En la carpeta Datos de la *aplicación,\_* cree un nuevo archivo XML denominado *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="9bfe0-175">Reemplace el XML existente por el siguiente, que son algunos datos XML sobre los empleados de una empresa ficticia.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="9bfe0-176">En la carpeta Datos de la *aplicación,\_* cree un nuevo archivo XML denominado *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="9bfe0-177">(Tenga en cuenta que la extensión esta vez es *.xsd*.)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="9bfe0-178">Reemplace el XML existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="9bfe0-179">En la raíz del sitio web, cree un nuevo archivo denominado *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="9bfe0-180">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="9bfe0-181">El código crea `DataSet` primero un objeto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="9bfe0-182">Este objeto se utiliza para administrar los datos que se leen desde el archivo XML y organizarlos según la información del archivo de esquema.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="9bfe0-183">(Tenga en cuenta que la `using SystemData`parte superior del código incluye la instrucción .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="9bfe0-184">Esto es necesario para poder trabajar `DataSet` con el objeto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="9bfe0-185">Para obtener más [ &quot;&quot; ](#SB_UsingStatements) información, consulte Uso de instrucciones y nombres completos más adelante en este artículo.)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="9bfe0-186">A continuación, el `DataView` código crea un objeto basado en el conjunto de datos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="9bfe0-187">La vista de datos proporciona un objeto que el gráfico puede enlazar a &#8212; es decir, leer y trazar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="9bfe0-188">El gráfico se enlaza a `AddSeries` los datos mediante el método, como se vio anteriormente al trazar los datos de la matriz, excepto que esta vez los `xValue` parámetros y `yValues` se establecen en el `DataView` objeto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="9bfe0-189">En este ejemplo también se muestra cómo especificar un tipo de gráfico determinado.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="9bfe0-190">Cuando se agregan los `AddSeries` datos `chartType` en el método, el parámetro también se establece para mostrar un gráfico circular.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="9bfe0-191">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="9bfe0-192">Declaraciones de "uso" y nombres completos</span><span class="sxs-lookup"><span data-stu-id="9bfe0-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="9bfe0-193">.NET Framework que ASP.NET páginas Web con sintaxis DeRazor se basa en muchos miles de componentes (clases).</span><span class="sxs-lookup"><span data-stu-id="9bfe0-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="9bfe0-194">Para que sea manejable trabajar con todas estas clases, se organizan en espacios de *nombres,* que son algo así como bibliotecas.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="9bfe0-195">Por ejemplo, `System.Web` el espacio de nombres contiene `System.Xml` clases que admiten la comunicación entre `System.Data` el explorador y el servidor, el espacio de nombres contiene clases que se usan para crear y leer archivos XML y el espacio de nombres contiene clases que permiten trabajar con datos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="9bfe0-196">Para tener acceso a cualquier clase determinada en .NET Framework, el código debe conocer no solo el nombre de clase, sino también el espacio de nombres en el que se encuentra la clase.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="9bfe0-197">Por ejemplo, para utilizar `Chart` la aplicación auxiliar, `System.Web.Helpers.Chart` el código necesita encontrar`System.Web.Helpers`la clase,`Chart`que combina el espacio de nombres ( ) con el nombre de clase ( ).</span><span class="sxs-lookup"><span data-stu-id="9bfe0-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="9bfe0-198">Esto se conoce como nombre *completo de* la clase &#8212; su ubicación completa e inequívoca dentro de la inmensidad de .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="9bfe0-199">En el código, esto tendría el siguiente aspecto:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="9bfe0-200">Sin embargo, es engorroso (y propenso a errores) tener que usar estos nombres largos y completos cada vez que desee hacer referencia a una clase o ayudante.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="9bfe0-201">Por lo tanto, para facilitar el uso de nombres de clase, puede *importar* los espacios de nombres que le interesan, que normalmente es solo un puñado de entre los muchos espacios de nombres de .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="9bfe0-202">Si ha importado un espacio de nombres, puede`Chart`usar solo un nombre`System.Web.Helpers.Chart`de clase ( ) en lugar del nombre completo ( ).</span><span class="sxs-lookup"><span data-stu-id="9bfe0-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="9bfe0-203">Cuando el código se ejecuta y encuentra un nombre de clase, puede buscar solo en los espacios de nombres que ha importado para encontrar esa clase.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="9bfe0-204">Cuando se usa ASP.NET páginas Web con sintaxis de Razor para crear páginas `WebPage` web, normalmente se usa el mismo conjunto de clases cada vez, incluida la clase, las distintas aplicaciones auxiliares, etc.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="9bfe0-205">Para ahorrarle el trabajo de importar los espacios de nombres relevantes cada vez que crea un sitio web, ASP.NET está configurado para que importe automáticamente un conjunto de espacios de nombres principales para cada sitio web.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="9bfe0-206">Es por eso que no ha tenido que lidiar con espacios de nombres o importar hasta ahora; Todas las clases con las que ha trabajado están en espacios de nombres que ya se han importado para usted.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="9bfe0-207">Sin embargo, a veces es necesario trabajar con una clase que no está en un espacio de nombres que se importa automáticamente.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="9bfe0-208">En ese caso, puede usar el nombre completo de esa clase o importar manualmente el espacio de nombres que contiene la clase.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="9bfe0-209">Para importar un espacio `using` de`import` nombres, use la instrucción (en Visual Basic), como se vio en un ejemplo anterior del artículo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="9bfe0-210">Por ejemplo, `DataSet` la clase `System.Data` está en el espacio de nombres.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="9bfe0-211">El `System.Data` espacio de nombres no está disponible automáticamente para ASP.NET páginas Razor.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="9bfe0-212">Por lo tanto, `DataSet` para trabajar con la clase usando su nombre completo, puede usar código como este:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="9bfe0-213">Si tiene que `DataSet` utilizar la clase repetidamente, puede importar un espacio de nombres como este y, a continuación, usar solo el nombre de clase en el código:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="9bfe0-214">Puede agregar `using` instrucciones para cualquier otro espacio de nombres de .NET Framework al que desee hacer referencia.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="9bfe0-215">Sin embargo, como se ha indicado, no tendrá que hacer esto con frecuencia, porque la mayoría de las clases con las que trabajará se encuentran en espacios de nombres que se importan automáticamente por ASP.NET para su uso en *páginas .cshtml* y *.vbhtml.*</span><span class="sxs-lookup"><span data-stu-id="9bfe0-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="9bfe0-216">Visualización de gráficos dentro de una página web</span><span class="sxs-lookup"><span data-stu-id="9bfe0-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="9bfe0-217">En los ejemplos que ha visto hasta ahora, se crea un gráfico y, a continuación, el gráfico se representa directamente en el explorador como un gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="9bfe0-218">En muchos casos, sin embargo, desea mostrar un gráfico como parte de una página, no sólo por sí mismo en el navegador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="9bfe0-219">Para ello se requiere un proceso de dos pasos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-219">To do that requires a two-step process.</span></span> <span data-ttu-id="9bfe0-220">El primer paso es crear una página que genere el gráfico, como ya ha visto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="9bfe0-221">El segundo paso es mostrar la imagen resultante en otra página.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="9bfe0-222">Para mostrar la imagen, `<img>` utilice un elemento HTML, de la misma manera que lo haría para mostrar cualquier imagen.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="9bfe0-223">Sin embargo, en lugar de hacer referencia `<img>` a un archivo *.jpg* o `Chart` *.png,* el elemento hace referencia al archivo *.cshtml* que contiene la aplicación auxiliar que crea el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9bfe0-224">Cuando se ejecuta la `<img>` página para mostrar, `Chart` el elemento obtiene la salida de la aplicación auxiliar y representa el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="9bfe0-225">Cree un archivo denominado *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="9bfe0-226">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="9bfe0-227">El código `<img>` utiliza el elemento para mostrar el gráfico que creó anteriormente en el *ChartArrayBasic.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="9bfe0-228">Ejecute la página web en un explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-228">Run the web page in a browser.</span></span> <span data-ttu-id="9bfe0-229">El *ShowChart.cshtml* archivo muestra la imagen del gráfico basado en el código contenido en el *ChartArrayBasic.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="9bfe0-230">Estilizar un gráfico</span><span class="sxs-lookup"><span data-stu-id="9bfe0-230">Styling a Chart</span></span>

<span data-ttu-id="9bfe0-231">La `Chart` aplicación auxiliar admite un gran número de opciones que le permiten personalizar la apariencia del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="9bfe0-232">Puede establecer colores, fuentes, bordes, etc.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="9bfe0-233">Una manera fácil de personalizar la apariencia de un gráfico es utilizar un *tema.*</span><span class="sxs-lookup"><span data-stu-id="9bfe0-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="9bfe0-234">Los temas son colecciones de información que especifican cómo presentar un gráfico utilizando fuentes, colores, etiquetas, paletas, bordes y efectos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="9bfe0-235">(Tenga en cuenta que el estilo de un gráfico no indica el tipo de gráfico.)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="9bfe0-236">En la tabla siguiente se enumeran los temas integrados.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="9bfe0-237">Tema</span><span class="sxs-lookup"><span data-stu-id="9bfe0-237">Theme</span></span> | <span data-ttu-id="9bfe0-238">Descripción</span><span class="sxs-lookup"><span data-stu-id="9bfe0-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="9bfe0-239">Muestra columnas rojas sobre un fondo blanco.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="9bfe0-240">Muestra columnas azules sobre un fondo degradado azul.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="9bfe0-241">Muestra columnas azules sobre un fondo degradado verde.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="9bfe0-242">Muestra columnas naranjas sobre un fondo degradado amarillo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="9bfe0-243">Muestra columnas rojas 3D sobre un fondo blanco.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="9bfe0-244">Puede especificar el tema que se utilizará al crear un nuevo gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="9bfe0-245">Cree un nuevo archivo denominado *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="9bfe0-246">Reemplace el contenido existente en la página por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="9bfe0-247">Este código es el mismo que el ejemplo anterior que usa `theme` la base `Chart` de datos para los datos, pero agrega el parámetro cuando crea el objeto.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="9bfe0-248">A continuación se muestra el código modificado:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="9bfe0-249">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-249">Run the page in a browser.</span></span> <span data-ttu-id="9bfe0-250">Verá los mismos datos que antes, pero el gráfico se ve más pulido:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="9bfe0-251">Guardar un gráfico</span><span class="sxs-lookup"><span data-stu-id="9bfe0-251">Saving a Chart</span></span>

<span data-ttu-id="9bfe0-252">Cuando usa `Chart` la aplicación auxiliar como ha visto hasta ahora en este artículo, la aplicación auxiliar vuelve a crear el gráfico desde cero cada vez que se invoca.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="9bfe0-253">Si es necesario, el código del gráfico también vuelve a consultar la base de datos o vuelve a leer el archivo XML para obtener los datos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="9bfe0-254">En algunos casos, hacer esto puede ser una operación compleja, como si la base de datos que está consultando es grande o si el archivo XML contiene una gran cantidad de datos.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="9bfe0-255">Incluso si el gráfico no implica una gran cantidad de datos, el proceso de creación dinámica de una imagen ocupa los recursos del servidor, y si muchas personas solicitan la página o páginas que muestran el gráfico, puede haber un impacto en el rendimiento de su sitio web.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="9bfe0-256">Para ayudarle a reducir el impacto potencial en el rendimiento de la creación de un gráfico, puede crear un gráfico la primera vez que lo necesite y, a continuación, guardarlo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="9bfe0-257">Cuando se necesita el gráfico de nuevo, en lugar de regenerarlo, puede recuperar la versión guardada y representarla.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="9bfe0-258">Puede guardar un gráfico de las siguientes maneras:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="9bfe0-259">Almacene en caché el gráfico en la memoria del equipo (en el servidor).</span><span class="sxs-lookup"><span data-stu-id="9bfe0-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="9bfe0-260">Guarde el gráfico como un archivo de imagen.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="9bfe0-261">Guarde el gráfico como un archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-261">Save the chart as an XML file.</span></span> <span data-ttu-id="9bfe0-262">Esta opción le permite modificar el gráfico antes de guardarlo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="9bfe0-263">Almacenamiento en caché de un gráfico</span><span class="sxs-lookup"><span data-stu-id="9bfe0-263">Caching a Chart</span></span>

<span data-ttu-id="9bfe0-264">Después de crear un gráfico, puede almacenarlo en caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="9bfe0-265">Almacenar en caché un gráfico significa que no es necesario volver a crearlo si es necesario volver a mostrarlo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="9bfe0-266">Cuando guarda un gráfico en la memoria caché, le da una clave que debe ser única para ese gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="9bfe0-267">Los gráficos guardados en la memoria caché podrían eliminarse si el servidor se queda sin memoria.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="9bfe0-268">Además, la memoria caché se borra si la aplicación se reinicia por cualquier motivo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="9bfe0-269">Por lo tanto, la forma estándar de trabajar con un gráfico almacenado en caché es comprobar siempre primero si está disponible en la memoria caché y, si no, crearlo o volver a crearlo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="9bfe0-270">En la raíz de su sitio web, cree un archivo denominado *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="9bfe0-271">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="9bfe0-272">La `<img>` etiqueta `src` incluye un atributo que apunta al archivo *ChartSaveToCache.cshtml* y pasa una clave a la página como una cadena de consulta.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="9bfe0-273">La clave contiene &quot;el&quot;valor myChartKey .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="9bfe0-274">El *ChartSaveToCache.cshtml* archivo `Chart` contiene la aplicación auxiliar que crea el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9bfe0-275">Creará esta página en un momento.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="9bfe0-276">Al final de la página hay un vínculo a una página denominada *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="9bfe0-277">Esa es una página que también creará en breve.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="9bfe0-278">Necesita el *ClearCache.cshtml* solo para probar el almacenamiento en caché para este ejemplo, no es un vínculo o página que normalmente incluiría al trabajar con gráficos almacenados en caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="9bfe0-279">En la raíz de su sitio web, cree un nuevo archivo denominado *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="9bfe0-280">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="9bfe0-281">El código comprueba primero si se pasó algo como valor de clave en la cadena de consulta.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="9bfe0-282">Si es así, el código intenta leer un `GetFromCache` gráfico fuera de la memoria caché llamando al método y pasándole la clave.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="9bfe0-283">Si resulta que no hay nada en la memoria caché bajo esa clave (lo que ocurriría la primera vez que se solicita el gráfico), el código crea el gráfico como de costumbre.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="9bfe0-284">Cuando el gráfico ha terminado, el código lo `SaveToCache`guarda en la memoria caché llamando a .</span><span class="sxs-lookup"><span data-stu-id="9bfe0-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="9bfe0-285">Ese método requiere una clave (por lo que el gráfico se puede solicitar más adelante) y la cantidad de tiempo que el gráfico debe guardarse en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="9bfe0-286">(La hora exacta en la que almacenaría en caché un gráfico dependerá de la frecuencia con la que pensaba que los datos que representa podrían cambiar.) El `SaveToCache` método también `slidingExpiration` requiere un parámetro &#8212; si se establece en true, el contador de tiempo de espera se restablece cada vez que se accede al gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="9bfe0-287">En este caso, en efecto significa que la entrada de caché del gráfico expira 2 minutos después de la última vez que alguien accedió al gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="9bfe0-288">(La alternativa a la expiración deslizante es la expiración absoluta, lo que significa que la entrada de caché expiraría exactamente 2 minutos después de que se puso en la memoria caché, sin importar la frecuencia con la que se había accedido.)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="9bfe0-289">Por último, el `WriteFromCache` código utiliza el método para capturar y representar el gráfico desde la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="9bfe0-290">Tenga en cuenta que `if` este método está fuera del bloque que comprueba la memoria caché, ya que obtendrá el gráfico de la memoria caché si el gráfico estaba allí para comenzar o tenía que generarse y guardarse en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="9bfe0-291">Observe que en el `AddTitle` ejemplo, el método incluye una marca de tiempo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="9bfe0-292">(Agrega la fecha y hora `DateTime.Now` actuales &#8212; &#8212; al título.)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="9bfe0-293">Cree una nueva página denominada *ClearCache.cshtml* y reemplace su contenido por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="9bfe0-294">Esta página `WebCache` utiliza la aplicación auxiliar para quitar el gráfico almacenado en caché en *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="9bfe0-295">Como se indicó anteriormente, normalmente no tiene que tener una página como esta.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="9bfe0-296">Lo está creando aquí solo para facilitar la prueba del almacenamiento en caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="9bfe0-297">Ejecute la página web *ShowCachedChart.cshtml* en un explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="9bfe0-298">La página muestra la imagen del gráfico en función del código contenido en el *ChartSaveToCache.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="9bfe0-299">Tome nota de lo que dice la marca de tiempo en el título del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descripción: Imagen del gráfico básico con marca de tiempo en el título del gráfico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="9bfe0-301">Cierre el explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-301">Close the browser.</span></span>
8. <span data-ttu-id="9bfe0-302">Ejecute el *ShowCachedChart.cshtml* de nuevo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="9bfe0-303">Observe que la marca de tiempo es la misma que antes, lo que indica que el gráfico no se ha regenerado, sino que se leyó desde la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="9bfe0-304">En *ShowCachedChart.cshtml*, haga clic en el vínculo **Borrar caché.**</span><span class="sxs-lookup"><span data-stu-id="9bfe0-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="9bfe0-305">Esto le lleva a *ClearCache.cshtml*, que informa de que se ha borrado la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="9bfe0-306">Haga clic en el **volver a ShowCachedChart.cshtml** enlace, o vuelva a ejecutar *ShowCachedChart.cshtml* desde WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="9bfe0-307">Observe que esta vez la marca de tiempo ha cambiado, porque la memoria caché se ha borrado.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="9bfe0-308">Por lo tanto, el código tenía que regenerar el gráfico y volver a colocarlo en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="9bfe0-309">Guardar un gráfico como un archivo de imagen</span><span class="sxs-lookup"><span data-stu-id="9bfe0-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="9bfe0-310">También puede guardar un gráfico como un archivo de imagen (por ejemplo, como un archivo *.jpg)* en el servidor.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="9bfe0-311">A continuación, puede utilizar el archivo de imagen de la forma en que lo haría con cualquier imagen.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="9bfe0-312">La ventaja es que el archivo se almacena en lugar de guardarse en una memoria caché temporal.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="9bfe0-313">Puede guardar una nueva imagen de gráfico en diferentes momentos (por ejemplo, cada hora) y, a continuación, mantener un registro permanente de los cambios que se producen a lo largo del tiempo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="9bfe0-314">Tenga en cuenta que debe asegurarse de que la aplicación web tiene permiso para guardar un archivo en la carpeta del servidor donde desea colocar el archivo de imagen.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9bfe0-315">En la raíz de su sitio web, cree una carpeta denominada \* \_ChartFiles\* si aún no existe.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="9bfe0-316">En la raíz de su sitio web, cree un nuevo archivo denominado *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="9bfe0-317">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="9bfe0-318">El código comprueba primero si el archivo *.jpg* `File.Exists` existe llamando al método.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9bfe0-319">Si el archivo no existe, el `Chart` código crea un nuevo a partir de una matriz.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="9bfe0-320">Esta vez, el `Save` código llama al `path` método y pasa el parámetro para especificar la ruta de acceso del archivo y el nombre de archivo de dónde guardar el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="9bfe0-321">En el cuerpo de `<img>` la página, un elemento utiliza la ruta de acceso para apuntar al archivo *.jpg* que se va a mostrar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="9bfe0-322">Ejecute el *ChartSave.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="9bfe0-323">Vuelva a WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-323">Return to WebMatrix.</span></span> <span data-ttu-id="9bfe0-324">Observe que se ha guardado un archivo de imagen denominado *chart01.jpg* en la \* \_carpeta ChartFiles.\*</span><span class="sxs-lookup"><span data-stu-id="9bfe0-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="9bfe0-325">Guardar un gráfico como un archivo XML</span><span class="sxs-lookup"><span data-stu-id="9bfe0-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="9bfe0-326">Por último, puede guardar un gráfico como un archivo XML en el servidor.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="9bfe0-327">Una ventaja de usar este método sobre el almacenamiento en caché del gráfico o guardar el gráfico en un archivo es que podría modificar el XML antes de mostrar el gráfico si lo desea.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="9bfe0-328">La aplicación debe tener permisos de lectura y escritura para la carpeta en el servidor donde desea colocar el archivo de imagen.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9bfe0-329">En la raíz de su sitio web, cree un nuevo archivo denominado *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="9bfe0-330">Reemplace el contenido existente por el siguiente:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="9bfe0-331">Este código es similar al código que vio anteriormente para almacenar un gráfico en la memoria caché, excepto que usa un archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="9bfe0-332">El código comprueba primero si el archivo XML `File.Exists` existe llamando al método.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9bfe0-333">Si el archivo existe, el `Chart` código crea un nuevo objeto `themePath` y pasa el nombre de archivo como parámetro.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="9bfe0-334">Esto crea el gráfico basado en lo que hay en el archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="9bfe0-335">Si el archivo XML aún no existe, el código crea `SaveXml` un gráfico como normal y, a continuación, llama para guardarlo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="9bfe0-336">El gráfico se representa `Write` mediante el método, como se ha visto antes.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="9bfe0-337">Al igual que con la página que mostraba el almacenamiento en caché, este código incluye una marca de tiempo en el título del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="9bfe0-338">Cree una nueva página denominada *ChartDisplayXMLChart.cshtml* y agréguele el siguiente marcado:</span><span class="sxs-lookup"><span data-stu-id="9bfe0-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="9bfe0-339">Ejecute la página *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9bfe0-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="9bfe0-340">Se muestra el gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-340">The chart is displayed.</span></span> <span data-ttu-id="9bfe0-341">Tome nota de la marca de tiempo en el título del gráfico.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="9bfe0-342">Cierre el explorador.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-342">Close the browser.</span></span>
6. <span data-ttu-id="9bfe0-343">En WebMatrix, haga clic con el **Refresh**botón secundario en la \* \_carpeta ChartFiles,\* haga clic en Actualizar y, a continuación, abra la carpeta.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="9bfe0-344">El archivo *XMLChart.xml* de esta `Chart` carpeta fue creado por la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descripción: _ChartFiles carpeta que muestra el archivo XMLChart.xml creado por la aplicación auxiliar Chart.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="9bfe0-346">Ejecute el *ChartDisplayXMLChart.cshtml* página de nuevo.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="9bfe0-347">El gráfico muestra la misma marca de tiempo que la primera vez que ejecutó la página.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="9bfe0-348">Esto se debe a que el gráfico se genera a partir del XML que guardó anteriormente.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="9bfe0-349">En WebMatrix, \* \_\* abra la carpeta ChartFiles y elimine el archivo *XMLChart.xml.*</span><span class="sxs-lookup"><span data-stu-id="9bfe0-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="9bfe0-350">Ejecute el *ChartDisplayXMLChart.cshtml* página una vez más.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="9bfe0-351">Esta vez, la marca de `Chart` tiempo se actualiza, porque la aplicación auxiliar tenía que volver a crear el archivo XML.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="9bfe0-352">Si lo desea, \* \_\* compruebe la carpeta ChartFiles y observe que el archivo XML está de vuelta.</span><span class="sxs-lookup"><span data-stu-id="9bfe0-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="9bfe0-353">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="9bfe0-353">Additional Resources</span></span>

- [<span data-ttu-id="9bfe0-354">Introducción al trabajo con una base de datos en ASP.NET sitios de páginas web</span><span class="sxs-lookup"><span data-stu-id="9bfe0-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="9bfe0-355">Uso del almacenamiento en caché en ASP.NET sitios de páginas web para mejorar el rendimiento</span><span class="sxs-lookup"><span data-stu-id="9bfe0-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="9bfe0-356">[Clase Chart](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET referencia de la API web Web Pages en MSDN)</span><span class="sxs-lookup"><span data-stu-id="9bfe0-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
