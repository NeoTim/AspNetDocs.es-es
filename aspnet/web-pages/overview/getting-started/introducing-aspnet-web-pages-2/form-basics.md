---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Presentación de ASP.NET Páginas Web - Conceptos básicos de formularios HTML Microsoft Docs
author: Rick-Anderson
description: En este tutorial se muestran los conceptos básicos de cómo crear un formulario de entrada y cómo controlar la entrada del usuario cuando se usa ASP.NET páginas web (Razor). Y ahora que...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/06/2020
ms.locfileid: "80675926"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="f3b67-104">Presentación de ASP.NET Web Pages - HTML Form Basics</span><span class="sxs-lookup"><span data-stu-id="f3b67-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="f3b67-105"> por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="f3b67-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="f3b67-106">En este tutorial se muestran los conceptos básicos de cómo crear un formulario de entrada y cómo controlar la entrada del usuario cuando se usa ASP.NET páginas web (Razor).</span><span class="sxs-lookup"><span data-stu-id="f3b67-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="f3b67-107">Y ahora que tienes una base de datos, usarás tus habilidades de formulario para permitir que los usuarios encuentren películas específicas en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="f3b67-108">Se supone que ha completado la serie a través de [Introducción a la visualización](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)de datos mediante ASP.NET páginas web .</span><span class="sxs-lookup"><span data-stu-id="f3b67-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="f3b67-109">Temas que se abordarán:</span><span class="sxs-lookup"><span data-stu-id="f3b67-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="f3b67-110">Cómo crear un formulario mediante elementos HTML estándar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="f3b67-111">Cómo leer la entrada del usuario en un formulario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="f3b67-112">Cómo crear una consulta SQL que obtiene datos de forma selectiva mediante un término de búsqueda que proporciona el usuario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="f3b67-113">Cómo tener campos en la página "recordar" lo que el usuario ingresó.</span><span class="sxs-lookup"><span data-stu-id="f3b67-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="f3b67-114">Características/tecnologías discutidas:</span><span class="sxs-lookup"><span data-stu-id="f3b67-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="f3b67-115">Objeto `Request`.</span><span class="sxs-lookup"><span data-stu-id="f3b67-115">The `Request` object.</span></span>
> - <span data-ttu-id="f3b67-116">La `Where` cláusula SQL.</span><span class="sxs-lookup"><span data-stu-id="f3b67-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="f3b67-117">Lo que creará</span><span class="sxs-lookup"><span data-stu-id="f3b67-117">What You'll Build</span></span>

<span data-ttu-id="f3b67-118">En el tutorial anterior, creó una base de datos, le agregó datos y, a continuación, usó la `WebGrid` aplicación auxiliar para mostrar los datos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="f3b67-119">En este tutorial, agregará un cuadro de búsqueda que le permite encontrar películas de un género específico o cuyo título contiene cualquier palabra que introduzca.</span><span class="sxs-lookup"><span data-stu-id="f3b67-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="f3b67-120">(Por ejemplo, podrás encontrar todas las películas cuyo género sea "Acción" o cuyo título contenga "Harry" o "Aventura")..</span><span class="sxs-lookup"><span data-stu-id="f3b67-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="f3b67-121">Cuando termines con este tutorial, tendrás una página como esta:</span><span class="sxs-lookup"><span data-stu-id="f3b67-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Página de películas con búsqueda de género y título](form-basics/_static/image1.png)

<span data-ttu-id="f3b67-123">La parte de la lista de la &mdash; página es la misma que en el último tutorial una cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="f3b67-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="f3b67-124">La diferencia será que la cuadrícula mostrará solo las películas que ha buscado.</span><span class="sxs-lookup"><span data-stu-id="f3b67-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="f3b67-125">Acerca de los formularios HTML</span><span class="sxs-lookup"><span data-stu-id="f3b67-125">About HTML Forms</span></span>

<span data-ttu-id="f3b67-126">(Si tiene experiencia en la creación de formularios `GET` `POST`HTML y con la diferencia entre y , puede omitir esta sección.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="f3b67-127">Un formulario tiene &mdash; cuadros de texto de elementos de entrada de usuario, botones, botones de opción, casillas de verificación, listas desplegables, etc.</span><span class="sxs-lookup"><span data-stu-id="f3b67-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="f3b67-128">Los usuarios rellenan estos controles o realizan selecciones y, a continuación, envían el formulario haciendo clic en un botón.</span><span class="sxs-lookup"><span data-stu-id="f3b67-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="f3b67-129">La sintaxis HTML básica de un formulario se ilustra en este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="f3b67-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="f3b67-130">Cuando este marcado se ejecuta en una página, crea un formulario simple similar a esta ilustración:</span><span class="sxs-lookup"><span data-stu-id="f3b67-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulario HTML básico tal como se representa en el navegador](form-basics/_static/image2.png)

<span data-ttu-id="f3b67-132">El `<form>` elemento encierra los elementos HTML que se van a enviar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="f3b67-133">(Un error fácil de cometer es agregar elementos a la `<form>` página, pero luego olvidarse de ponerlos dentro de un elemento.</span><span class="sxs-lookup"><span data-stu-id="f3b67-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="f3b67-134">En ese caso, no se presenta nada.) El `method` atributo indica al explorador cómo enviar la entrada del usuario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="f3b67-135">Establezca esta `post` configuración si va a realizar una actualización `get` en el servidor o en si solo está obteniendo datos del servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="f3b67-136">**GET, POST y HTTP Verb Safety**</span><span class="sxs-lookup"><span data-stu-id="f3b67-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="f3b67-137">HTTP, el protocolo que utilizan los navegadores y servidores para intercambiar información, es notablemente simple en sus operaciones básicas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="f3b67-138">Los navegadores utilizan solo unos pocos verbos para realizar solicitudes a los servidores.</span><span class="sxs-lookup"><span data-stu-id="f3b67-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="f3b67-139">Al escribir código para la web, es útil comprender estos verbos y cómo los utilizan el navegador y el servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="f3b67-140">Lejos y lejos los verbos más utilizados son los siguientes:</span><span class="sxs-lookup"><span data-stu-id="f3b67-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="f3b67-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="f3b67-141">`GET`.</span></span> <span data-ttu-id="f3b67-142">El navegador utiliza este verbo para obtener algo del servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="f3b67-143">Por ejemplo, al escribir una dirección URL en `GET` el explorador, el explorador realiza una operación para solicitar la página que desea.</span><span class="sxs-lookup"><span data-stu-id="f3b67-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="f3b67-144">Si la página incluye gráficos, `GET` el explorador realiza operaciones adicionales para obtener las imágenes.</span><span class="sxs-lookup"><span data-stu-id="f3b67-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="f3b67-145">Si `GET` la operación tiene que pasar información al servidor, la información se pasa como parte de la dirección URL de la cadena de consulta.</span><span class="sxs-lookup"><span data-stu-id="f3b67-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="f3b67-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="f3b67-146">`POST`.</span></span> <span data-ttu-id="f3b67-147">El navegador `POST` envía una petición para enviar los datos para ser agregados o cambiados en el servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="f3b67-148">Por ejemplo, `POST` el verbo se utiliza para crear registros en una base de datos o cambiar los existentes.</span><span class="sxs-lookup"><span data-stu-id="f3b67-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="f3b67-149">La mayoría de las veces, cuando rellena un formulario y `POST` hace clic en el botón Enviar, el explorador realiza una operación.</span><span class="sxs-lookup"><span data-stu-id="f3b67-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="f3b67-150">En `POST` una operación, los datos que se pasan al servidor se encuentra en el cuerpo de la página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="f3b67-151">Una distinción importante entre estos `GET` verbos es que una operación no se supone que cambie `GET` nada en el servidor, o para ponerlo de una manera un poco más abstracta, una operación no da lugar a un cambio de estado en el servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="f3b67-152">Puede realizar `GET` una operación con los mismos recursos tantas veces como desee y esos recursos no cambian.</span><span class="sxs-lookup"><span data-stu-id="f3b67-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="f3b67-153">(A `GET` menudo se dice que una operación es "segura", o que utiliza un término técnico, es *idempotente*.) Por el contrario, `POST` por supuesto, una solicitud cambia algo en el servidor cada vez que realiza la operación.</span><span class="sxs-lookup"><span data-stu-id="f3b67-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="f3b67-154">Dos ejemplos ayudarán a ilustrar esta distinción.</span><span class="sxs-lookup"><span data-stu-id="f3b67-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="f3b67-155">Cuando realiza una búsqueda con un motor como Bing o Google, rellena un formulario que consta de un cuadro de texto y, a continuación, hace clic en el botón de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="f3b67-156">El explorador realiza `GET` una operación, con el valor especificado en el cuadro pasado como parte de la dirección URL.</span><span class="sxs-lookup"><span data-stu-id="f3b67-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="f3b67-157">El `GET` uso de una operación para este tipo de formulario está bien, ya que una operación de búsqueda no cambia ningún recurso en el servidor, solo recupera información.</span><span class="sxs-lookup"><span data-stu-id="f3b67-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="f3b67-158">Ahora considere el proceso de pedir algo en línea.</span><span class="sxs-lookup"><span data-stu-id="f3b67-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="f3b67-159">Rellene los detalles del pedido y, a continuación, haga clic en el botón Enviar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="f3b67-160">Esta operación `POST` será una solicitud, porque la operación dará lugar a cambios en el servidor, como un nuevo registro de pedido, un cambio en la información de su cuenta y tal vez muchos otros cambios.</span><span class="sxs-lookup"><span data-stu-id="f3b67-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="f3b67-161">A `GET` diferencia de la `POST` operación, no puede repetir su solicitud: si lo hizo, cada vez que vuelva a enviar la solicitud, generaría un nuevo pedido en el servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="f3b67-162">(En casos como este, los sitios web a menudo le advertirán que no haga clic en un botón de envío más de una vez, o desactivará el botón de envío para que no vuelva a enviar el formulario accidentalmente.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="f3b67-163">En el transcurso de este tutorial, `GET` usará `POST` una operación y una operación para trabajar con formularios HTML.</span><span class="sxs-lookup"><span data-stu-id="f3b67-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="f3b67-164">Explicaremos en cada caso por qué el verbo que usas es el adecuado.</span><span class="sxs-lookup"><span data-stu-id="f3b67-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="f3b67-165">(Para obtener más información sobre los verbos HTTP, consulte el artículo [Definiciones](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) de método en el sitio W3C.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="f3b67-166">La mayoría de `<input>` los elementos de entrada del usuario son elementos HTML.</span><span class="sxs-lookup"><span data-stu-id="f3b67-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="f3b67-167">Parecen `<input type="type" name="name">,` donde *el tipo* indica el tipo de control de entrada de usuario que desea.</span><span class="sxs-lookup"><span data-stu-id="f3b67-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="f3b67-168">Estos elementos son los comunes:</span><span class="sxs-lookup"><span data-stu-id="f3b67-168">These elements are the common ones:</span></span>

- <span data-ttu-id="f3b67-169">Cuadro de texto:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="f3b67-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="f3b67-170">Casilla de verificación:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="f3b67-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="f3b67-171">Botón de radio:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="f3b67-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="f3b67-172">Botón:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="f3b67-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="f3b67-173">Botón Enviar:`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="f3b67-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="f3b67-174">También puede utilizar `<textarea>` el elemento para crear un `<select>` cuadro de texto de varias líneas y el elemento para crear una lista desplegable o una lista desplazable.</span><span class="sxs-lookup"><span data-stu-id="f3b67-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="f3b67-175">(Para obtener más información sobre los elementos de formulario HTML, consulte [Formularios HTML y entrada](http://www.w3schools.com/html/html_forms.asp) en el sitio de W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="f3b67-176">El `name` atributo es muy importante, porque el nombre es cómo obtendrá el valor del elemento más adelante, como verá en breve.</span><span class="sxs-lookup"><span data-stu-id="f3b67-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="f3b67-177">La parte interesante es lo que usted, el desarrollador de la página, hace con la entrada del usuario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="f3b67-178">No hay ningún comportamiento integrado asociado con estos elementos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="f3b67-179">En su lugar, tiene que obtener los valores que el usuario ha introducido o seleccionado y hacer algo con ellos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="f3b67-180">Eso es lo que aprenderás en este tutorial.</span><span class="sxs-lookup"><span data-stu-id="f3b67-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="f3b67-181">**HTML5 y formularios de entrada**</span><span class="sxs-lookup"><span data-stu-id="f3b67-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="f3b67-182">Como usted puede saber, HTML está en transición y la última versión (HTML5) incluye soporte para formas más intuitivas para que los usuarios ingresen información.</span><span class="sxs-lookup"><span data-stu-id="f3b67-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="f3b67-183">Por ejemplo, en HTML5, usted (el desarrollador de la página) puede indicar a la página que desea que el usuario escriba una fecha.</span><span class="sxs-lookup"><span data-stu-id="f3b67-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="f3b67-184">El navegador puede mostrar automáticamente un calendario en lugar de requerir que el usuario introduzca una fecha manualmente.</span><span class="sxs-lookup"><span data-stu-id="f3b67-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="f3b67-185">Sin embargo, HTML5 es nuevo y no es compatible con todos los navegadores todavía.</span><span class="sxs-lookup"><span data-stu-id="f3b67-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="f3b67-186">ASP.NET Web Pages admite la entrada HTML5 en la medida en que lo hace el explorador del usuario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="f3b67-187">Para obtener una idea de `<input>` los nuevos atributos para el elemento en HTML5, consulte Tipo de [entrada &lt;&gt; HTML Atributo](http://www.w3schools.com/html/html_form_input_types.asp) en el sitio W3Schools.</span><span class="sxs-lookup"><span data-stu-id="f3b67-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="f3b67-188">Crear el formulario</span><span class="sxs-lookup"><span data-stu-id="f3b67-188">Creating the Form</span></span>

<span data-ttu-id="f3b67-189">En WebMatrix, en el **archivos** área de trabajo, abra el *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="f3b67-190">Después `</h1>` de la etiqueta `<div>` de `grid.GetHtml` cierre y antes de la etiqueta de apertura de la llamada, agregue el siguiente marcado:</span><span class="sxs-lookup"><span data-stu-id="f3b67-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="f3b67-191">Este marcado crea un formulario que `searchGenre` tiene un cuadro de texto denominado y un botón de envío.</span><span class="sxs-lookup"><span data-stu-id="f3b67-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="f3b67-192">El cuadro de texto y el `<form>` botón Enviar se incluyen en un elemento cuyo `method` atributo está establecido en `get`.</span><span class="sxs-lookup"><span data-stu-id="f3b67-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="f3b67-193">(Recuerde que si no coloca el cuadro de `<form>` texto y envía el botón dentro de un elemento, no se enviará nada al hacer clic en el botón.) Utilice el `GET` verbo aquí porque está creando un formulario que no realiza ningún cambio en el servidor, solo da como resultado una búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="f3b67-194">(En el tutorial anterior, `post` usó un método, que es cómo enviar los cambios al servidor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="f3b67-195">Lo verá en el siguiente tutorial de nuevo.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="f3b67-196">Ejecute la página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-196">Run the page.</span></span> <span data-ttu-id="f3b67-197">Aunque no ha definido ningún comportamiento para el formulario, puede ver cómo se ve:</span><span class="sxs-lookup"><span data-stu-id="f3b67-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Página De películas con cuadro de búsqueda para Género](form-basics/_static/image3.png)

<span data-ttu-id="f3b67-199">Introduzca un valor en el cuadro de texto, como "Comedia".</span><span class="sxs-lookup"><span data-stu-id="f3b67-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="f3b67-200">A continuación, haga clic en **Buscar género**.</span><span class="sxs-lookup"><span data-stu-id="f3b67-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="f3b67-201">Tome nota de la URL de la página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-201">Take note of the URL of the page.</span></span> <span data-ttu-id="f3b67-202">Dado que `<form>` establece el `method` atributo `get`del elemento en , el valor que ha especificado ahora forma parte de la cadena de consulta en la dirección URL, como esta:</span><span class="sxs-lookup"><span data-stu-id="f3b67-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="f3b67-203">Valores del formulario de lectura</span><span class="sxs-lookup"><span data-stu-id="f3b67-203">Reading Form Values</span></span>

<span data-ttu-id="f3b67-204">La página ya contiene código que obtiene datos de base de datos y muestra los resultados en una cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="f3b67-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="f3b67-205">Ahora tiene que agregar código que lea el valor del cuadro de texto para que pueda ejecutar una consulta SQL que incluya el término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="f3b67-206">Dado que establece el método `get`del formulario en , puede leer el valor que se ha especificado en el cuadro de texto mediante código como el siguiente:</span><span class="sxs-lookup"><span data-stu-id="f3b67-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="f3b67-207">El `Request.QueryString` objeto `QueryString` (la `Request` propiedad del objeto) incluye los valores de `GET` los elementos que se enviaron como parte de la operación.</span><span class="sxs-lookup"><span data-stu-id="f3b67-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="f3b67-208">La `Request.QueryString` propiedad contiene una *colección* (una lista) de los valores que se envían en el formulario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="f3b67-209">Para obtener cualquier valor individual, especifique el nombre del elemento que desee.</span><span class="sxs-lookup"><span data-stu-id="f3b67-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="f3b67-210">Es por eso que tiene `name` que `<input>` tener`searchTerm`un atributo en el elemento ( ) que crea el cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="f3b67-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="f3b67-211">(Para obtener `Request` más información sobre el objeto, consulte la [barra lateral](#BKMK_TheRequestObject) más adelante.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="f3b67-212">Es bastante simple leer el valor del cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="f3b67-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="f3b67-213">Pero si el usuario no ingresó nada en absoluto en el cuadro de texto, pero hizo clic en **Buscar** de todos modos, puede ignorar ese clic, ya que no hay nada que buscar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="f3b67-214">El código siguiente es un ejemplo que muestra cómo implementar estas condiciones.</span><span class="sxs-lookup"><span data-stu-id="f3b67-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="f3b67-215">(No tiene que agregar este código todavía; lo hará en un momento.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="f3b67-216">La prueba se descompone de esta manera:</span><span class="sxs-lookup"><span data-stu-id="f3b67-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="f3b67-217">Obtenga el `Request.QueryString["searchGenre"]`valor de , es decir, `<input>` el `searchGenre`valor que se ha especificado en el elemento denominado .</span><span class="sxs-lookup"><span data-stu-id="f3b67-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="f3b67-218">Averigua si está vacío usando `IsEmpty` el método.</span><span class="sxs-lookup"><span data-stu-id="f3b67-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="f3b67-219">Este método es la forma estándar de determinar si algo (por ejemplo, un elemento de formulario) contiene un valor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="f3b67-220">Pero en realidad, te importa sólo si *no* está vacío, por lo tanto ...</span><span class="sxs-lookup"><span data-stu-id="f3b67-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="f3b67-221">Agregue `!` el operador delante `IsEmpty` de la prueba.</span><span class="sxs-lookup"><span data-stu-id="f3b67-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="f3b67-222">(El `!` operador significa NOT lógico).</span><span class="sxs-lookup"><span data-stu-id="f3b67-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="f3b67-223">En inglés simple, toda `if` la condición se traduce en lo siguiente: Si el elemento *searchGenre del formulario no está vacío, entonces ...*</span><span class="sxs-lookup"><span data-stu-id="f3b67-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="f3b67-224">Este bloque establece el escenario para crear una consulta que usa el término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="f3b67-225">Lo hará en la sección siguiente.</span><span class="sxs-lookup"><span data-stu-id="f3b67-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="f3b67-226">**El objeto Request**</span><span class="sxs-lookup"><span data-stu-id="f3b67-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="f3b67-227">El `Request` objeto contiene toda la información que el explorador envía a la aplicación cuando se solicita o envía una página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="f3b67-228">Este objeto incluye cualquier información que el usuario proporcione, como valores de cuadro de texto o un archivo para cargar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="f3b67-229">También incluye todo tipo de información adicional, como cookies, valores en la cadena de consulta URL (si existe), la ruta de acceso del archivo de la página que se está ejecutando, el tipo de navegador que utiliza el usuario, la lista de idiomas que se establecen en el explorador y mucho más.</span><span class="sxs-lookup"><span data-stu-id="f3b67-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="f3b67-230">El `Request` objeto es una *colección* (lista) de valores.</span><span class="sxs-lookup"><span data-stu-id="f3b67-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="f3b67-231">Para obtener un valor individual de la colección, especifique su nombre:</span><span class="sxs-lookup"><span data-stu-id="f3b67-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="f3b67-232">El `Request` objeto expone realmente varios subconjuntos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="f3b67-233">Por ejemplo:</span><span class="sxs-lookup"><span data-stu-id="f3b67-233">For example:</span></span>
> 
> - <span data-ttu-id="f3b67-234">`Request.Form`proporciona valores de elementos `<form>` dentro del elemento `POST` enviado si la solicitud es una solicitud.</span><span class="sxs-lookup"><span data-stu-id="f3b67-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="f3b67-235">`Request.QueryString`le da solo los valores de la cadena de consulta de la dirección URL.</span><span class="sxs-lookup"><span data-stu-id="f3b67-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="f3b67-236">(En una `http://mysite/myapp/page?searchGenre=action&page=2`dirección `?searchGenre=action&page=2` URL como , la sección de la dirección URL es la cadena de consulta.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="f3b67-237">`Request.Cookies`colección le da acceso a las cookies que el navegador ha enviado.</span><span class="sxs-lookup"><span data-stu-id="f3b67-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="f3b67-238">Para obtener un valor que sabe que está `Request["name"]`en el formulario enviado, puede usar .</span><span class="sxs-lookup"><span data-stu-id="f3b67-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="f3b67-239">Como alternativa, puede utilizar las `Request.Form["name"]` versiones más específicas (para `POST` solicitudes) o `Request.QueryString["name"]` (para `GET` solicitudes).</span><span class="sxs-lookup"><span data-stu-id="f3b67-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="f3b67-240">Por supuesto, *el nombre* es el nombre del elemento que se va a obtener.</span><span class="sxs-lookup"><span data-stu-id="f3b67-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="f3b67-241">El nombre del elemento que desea obtener tiene que ser único dentro de la colección que está utilizando.</span><span class="sxs-lookup"><span data-stu-id="f3b67-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="f3b67-242">Es por eso `Request` que el objeto `Request.Form` `Request.QueryString`proporciona los subconjuntos like y .</span><span class="sxs-lookup"><span data-stu-id="f3b67-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="f3b67-243">Supongamos que la página `userName` contiene un elemento `userName`de formulario denominado y *también* contiene una cookie denominada .</span><span class="sxs-lookup"><span data-stu-id="f3b67-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="f3b67-244">Si obtiene `Request["userName"]`, es ambiguo si desea el valor del formulario o la cookie.</span><span class="sxs-lookup"><span data-stu-id="f3b67-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="f3b67-245">Sin embargo, `Request.Form["userName"]` `Request.Cookie["userName"]`si obtiene o , está siendo explícito sobre qué valor obtener.</span><span class="sxs-lookup"><span data-stu-id="f3b67-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="f3b67-246">Es una buena práctica ser específico y `Request` usar el subconjunto del `Request.Form` que `Request.QueryString`te interesa, como o .</span><span class="sxs-lookup"><span data-stu-id="f3b67-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="f3b67-247">Para las páginas simples que está creando en este tutorial, probablemente no haga ninguna diferencia.</span><span class="sxs-lookup"><span data-stu-id="f3b67-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="f3b67-248">Sin embargo, a medida que crea `Request.Form` `Request.QueryString` páginas más complejas, utilizando la versión explícita o puede ayudarle a evitar problemas que pueden surgir cuando la página contiene un formulario (o varios formularios), cookies, valores de cadena de consulta, etc.</span><span class="sxs-lookup"><span data-stu-id="f3b67-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="f3b67-249">Creación de una consulta mediante un término de búsqueda</span><span class="sxs-lookup"><span data-stu-id="f3b67-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="f3b67-250">Ahora que sabe cómo obtener el término de búsqueda que el usuario ha especificado, puede crear una consulta que lo use.</span><span class="sxs-lookup"><span data-stu-id="f3b67-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="f3b67-251">Recuerde que para obtener todos los elementos de película de la base de datos, está utilizando una consulta SQL que se parece a esta instrucción:</span><span class="sxs-lookup"><span data-stu-id="f3b67-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="f3b67-252">Para obtener solo determinadas películas, debe `Where` usar una consulta que incluya una cláusula.</span><span class="sxs-lookup"><span data-stu-id="f3b67-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="f3b67-253">Esta cláusula le permite establecer una condición en la que la consulta devuelve las filas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="f3b67-254">Este es un ejemplo:</span><span class="sxs-lookup"><span data-stu-id="f3b67-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="f3b67-255">El formato `WHERE column = value`básico es .</span><span class="sxs-lookup"><span data-stu-id="f3b67-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="f3b67-256">Puede utilizar diferentes operadores `=`además de , como `>` (mayor que), `<` (menor que), `<>` (no igual a), `<=` (menor o igual que), etc., dependiendo de lo que esté buscando.</span><span class="sxs-lookup"><span data-stu-id="f3b67-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="f3b67-257">En caso de que se pregunte, las &mdash; `SELECT` instrucciones SQL `Select` no `select`distinguen entre mayúsculas y minúsculas es la misma que (o incluso ).</span><span class="sxs-lookup"><span data-stu-id="f3b67-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="f3b67-258">Sin embargo, las personas a menudo `SELECT` capitalizan las palabras clave en una instrucción SQL, como y `WHERE`, para que sea más fácil de leer.</span><span class="sxs-lookup"><span data-stu-id="f3b67-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="f3b67-259">Pasar el término de búsqueda como parámetro</span><span class="sxs-lookup"><span data-stu-id="f3b67-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="f3b67-260">Buscar un género específico es`WHERE Genre = 'Action'`bastante fácil ( ), pero quieres poder buscar cualquier género que el usuario introduzca.</span><span class="sxs-lookup"><span data-stu-id="f3b67-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="f3b67-261">Para ello, cree como consulta SQL que incluya un marcador de posición para el valor que se va a buscar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="f3b67-262">Se verá como este comando:</span><span class="sxs-lookup"><span data-stu-id="f3b67-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="f3b67-263">El marcador `@` de posición es el carácter seguido de cero.</span><span class="sxs-lookup"><span data-stu-id="f3b67-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="f3b67-264">Como puede adivinar, una consulta puede contener varios marcadores `@0` `@1`de `@2`posición y se denominaría , , , etc.</span><span class="sxs-lookup"><span data-stu-id="f3b67-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="f3b67-265">Para configurar la consulta y pasarle realmente el valor, utilice el código como el siguiente:</span><span class="sxs-lookup"><span data-stu-id="f3b67-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="f3b67-266">Este código es similar a lo que ya ha hecho para mostrar datos en la cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="f3b67-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="f3b67-267">Las únicas diferencias son:</span><span class="sxs-lookup"><span data-stu-id="f3b67-267">The only differences are:</span></span>

- <span data-ttu-id="f3b67-268">La consulta contiene`WHERE Genre = @0"`un marcador de posición ( ).</span><span class="sxs-lookup"><span data-stu-id="f3b67-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="f3b67-269">La consulta se coloca`selectCommand`en una variable ( ); antes, pasó la consulta `db.Query` directamente al método.</span><span class="sxs-lookup"><span data-stu-id="f3b67-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="f3b67-270">Cuando se `db.Query` llama al método, se pasa la consulta y el valor que se usará para el marcador de posición.</span><span class="sxs-lookup"><span data-stu-id="f3b67-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="f3b67-271">(Si la consulta tuviera varios marcadores de posición, los pasaría todos como valores independientes al método.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="f3b67-272">Si junta todos estos elementos, obtendrá el siguiente código:</span><span class="sxs-lookup"><span data-stu-id="f3b67-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="f3b67-273">**¡Importante!**</span><span class="sxs-lookup"><span data-stu-id="f3b67-273">**Important!**</span></span> <span data-ttu-id="f3b67-274">El uso de `@0`marcadores de posición (como ) para pasar valores a un comando SQL es *extremadamente importante* para la seguridad.</span><span class="sxs-lookup"><span data-stu-id="f3b67-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="f3b67-275">La forma en que lo ve aquí, con marcadores de posición para datos variables, es la única manera de construir comandos SQL.</span><span class="sxs-lookup"><span data-stu-id="f3b67-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="f3b67-276">Nunca construya una instrucción SQL reuniendo (concatenando) texto literal y los valores que obtiene del usuario.</span><span class="sxs-lookup"><span data-stu-id="f3b67-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="f3b67-277">La concatenación de la entrada del usuario en una instrucción SQL abre el sitio a un ataque de *inyección SQL* en el que un usuario malintencionado envía valores a la página que hackea la base de datos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="f3b67-278">(Puede leer más en el artículo [Inyección SQL](https://msdn.microsoft.com/library/ms161953.aspx) el sitio web de MSDN.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="f3b67-279">Actualización de la página De las películas con código de búsqueda</span><span class="sxs-lookup"><span data-stu-id="f3b67-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="f3b67-280">Ahora puede actualizar el código en el *Movies.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="f3b67-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="f3b67-281">Para empezar, reemplace el código del bloque de código en la parte superior de la página con este código:</span><span class="sxs-lookup"><span data-stu-id="f3b67-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="f3b67-282">La diferencia aquí es que ha puesto `selectCommand` la consulta en la `db.Query` variable, que pasará más adelante.</span><span class="sxs-lookup"><span data-stu-id="f3b67-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="f3b67-283">Colocar la instrucción SQL en una variable le permite cambiar la instrucción, que es lo que hará para realizar la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="f3b67-284">También has eliminado estas dos líneas, que volverás a colocar más adelante:</span><span class="sxs-lookup"><span data-stu-id="f3b67-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="f3b67-285">Todavía no desea ejecutar la consulta (es `db.Query`decir, llame) y tampoco `WebGrid` desea inicializar la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="f3b67-286">Lo hará después de descubrir qué instrucción SQL tiene que ejecutarse.</span><span class="sxs-lookup"><span data-stu-id="f3b67-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="f3b67-287">Después de este bloque reescrito, puede agregar la nueva lógica para controlar la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="f3b67-288">El código completado tendrá el siguiente aspecto.</span><span class="sxs-lookup"><span data-stu-id="f3b67-288">The completed code will look like the following.</span></span> <span data-ttu-id="f3b67-289">Actualice el código de la página para que coincida con este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="f3b67-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="f3b67-290">La página ahora funciona así.</span><span class="sxs-lookup"><span data-stu-id="f3b67-290">The page now works like this.</span></span> <span data-ttu-id="f3b67-291">Cada vez que se ejecuta la página, el código abre la base de datos y la `selectCommand` variable se establece en la instrucción SQL que obtiene todos los registros de la `Movies` tabla.</span><span class="sxs-lookup"><span data-stu-id="f3b67-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="f3b67-292">El código también `searchTerm` inicializa la variable.</span><span class="sxs-lookup"><span data-stu-id="f3b67-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="f3b67-293">Sin embargo, si la solicitud `searchGenre` actual incluye `selectCommand` un valor para el elemento, el `Where` código se establece en una consulta diferente, es decir, en una que incluya la cláusula para buscar un género.</span><span class="sxs-lookup"><span data-stu-id="f3b67-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="f3b67-294">También se `searchTerm` establece en lo que se pasó para el cuadro de búsqueda (que podría ser nada).</span><span class="sxs-lookup"><span data-stu-id="f3b67-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="f3b67-295">Independientemente de la instrucción SQL `selectCommand`en `db.Query` , el código llama a `searchTerm`continuación para ejecutar la consulta, pasándole lo que sea en .</span><span class="sxs-lookup"><span data-stu-id="f3b67-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="f3b67-296">Si no hay `searchTerm`nada en , no importa, porque en ese caso no `selectCommand` hay ningún parámetro para pasar el valor de todos modos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="f3b67-297">Por último, el `WebGrid` código inicializa la aplicación auxiliar mediante los resultados de la consulta, como antes.</span><span class="sxs-lookup"><span data-stu-id="f3b67-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="f3b67-298">Puede ver que al colocar la instrucción SQL y el término de búsqueda en variables, ha agregado flexibilidad al código.</span><span class="sxs-lookup"><span data-stu-id="f3b67-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="f3b67-299">Como verá más adelante en este tutorial, puede usar este marco básico y seguir agregando lógica para diferentes tipos de búsquedas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="f3b67-300">Prueba de la función Buscar por género</span><span class="sxs-lookup"><span data-stu-id="f3b67-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="f3b67-301">En WebMatrix, ejecute el *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="f3b67-302">Verá la página con el cuadro de texto para el género.</span><span class="sxs-lookup"><span data-stu-id="f3b67-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="f3b67-303">Escriba un género que haya introducido para uno de los registros de prueba y, a continuación, haga clic en **Buscar**.</span><span class="sxs-lookup"><span data-stu-id="f3b67-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="f3b67-304">Esta vez verás una lista de las películas que coinciden con ese género:</span><span class="sxs-lookup"><span data-stu-id="f3b67-304">This time you see a listing of just the movies that match that genre:</span></span>

![Lista de páginas de películas después de buscar el género 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="f3b67-306">Introduzca un género diferente y vuelva a buscar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-306">Enter a different genre and search again.</span></span> <span data-ttu-id="f3b67-307">Intente introducir el género utilizando todas las letras minúsculas o mayúsculas para que pueda ver que la búsqueda no distingue mayúsculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="f3b67-308">"Recordar" lo que el usuario ingresó</span><span class="sxs-lookup"><span data-stu-id="f3b67-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="f3b67-309">Es posible que haya notado que después de entrar en un género y haga clic en **Buscar género**, vio un listado para ese género.</span><span class="sxs-lookup"><span data-stu-id="f3b67-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="f3b67-310">Sin embargo, el &mdash; cuadro de texto de búsqueda estaba vacío en otras palabras, no recordaba lo que había introducido.</span><span class="sxs-lookup"><span data-stu-id="f3b67-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="f3b67-311">Es importante entender por qué se produce este comportamiento.</span><span class="sxs-lookup"><span data-stu-id="f3b67-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="f3b67-312">Cuando envía una página, el explorador envía una solicitud al servidor web.</span><span class="sxs-lookup"><span data-stu-id="f3b67-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="f3b67-313">Cuando ASP.NET obtiene la solicitud, crea una nueva instancia de la página, ejecuta el código en ella y, a continuación, vuelve a representar la página en el explorador.</span><span class="sxs-lookup"><span data-stu-id="f3b67-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="f3b67-314">En efecto, sin embargo, la página no sabe que usted estaba trabajando con una versión anterior de sí mismo.</span><span class="sxs-lookup"><span data-stu-id="f3b67-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="f3b67-315">Todo lo que sabe es que recibió una solicitud que tenía algunos datos de formulario en ella.</span><span class="sxs-lookup"><span data-stu-id="f3b67-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="f3b67-316">Cada vez que &mdash; solicites una página, ya &mdash; sea por primera vez o enviándola, obtendrás una nueva página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="f3b67-317">El servidor web no tiene memoria de la última solicitud.</span><span class="sxs-lookup"><span data-stu-id="f3b67-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="f3b67-318">Tampoco ASP.NET, y tampoco lo hace el navegador.</span><span class="sxs-lookup"><span data-stu-id="f3b67-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="f3b67-319">La única conexión entre estas instancias independientes de la página es cualquier dato que se transmita entre ellas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="f3b67-320">Si envía una página, por ejemplo, la nueva instancia de página puede obtener los datos del formulario enviados por la instancia anterior.</span><span class="sxs-lookup"><span data-stu-id="f3b67-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="f3b67-321">(Otra forma de pasar datos entre páginas es utilizar cookies.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="f3b67-322">Una forma formal de describir esta situación es decir que las páginas web son *sin estado.*</span><span class="sxs-lookup"><span data-stu-id="f3b67-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="f3b67-323">Los servidores web y las propias páginas y los elementos de la página no mantienen ninguna información sobre el estado anterior de una página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="f3b67-324">La web fue diseñada de esta manera porque mantener el estado de las solicitudes individuales agotaría rápidamente los recursos de los servidores web, que a menudo manejan miles, tal vez incluso cientos de miles, de solicitudes por segundo.</span><span class="sxs-lookup"><span data-stu-id="f3b67-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="f3b67-325">Por eso el cuadro de texto estaba vacío.</span><span class="sxs-lookup"><span data-stu-id="f3b67-325">So that's why the text box was empty.</span></span> <span data-ttu-id="f3b67-326">Después de enviar la página, ASP.NET creó una nueva instancia de la página y corrió a través del código y el marcado.</span><span class="sxs-lookup"><span data-stu-id="f3b67-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="f3b67-327">No había nada en ese código que ASP.NET dijera que pusiera un valor en el cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="f3b67-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="f3b67-328">Así que ASP.NET no hizo nada, y el cuadro de texto se representó sin un valor en él.</span><span class="sxs-lookup"><span data-stu-id="f3b67-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="f3b67-329">En realidad, hay una manera fácil de evitar este problema.</span><span class="sxs-lookup"><span data-stu-id="f3b67-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="f3b67-330">El género que ha introducido en el cuadro &mdash; de texto `Request.QueryString["searchGenre"]` *está* disponible en el código en el que se encuentra.</span><span class="sxs-lookup"><span data-stu-id="f3b67-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="f3b67-331">Actualice el marcado del cuadro `value` de texto para `searchTerm`que el atributo obtenga su valor de , como en este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="f3b67-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="f3b67-332">En esta página, también podría `value` haber `searchTerm` establecido el atributo en la variable, ya que esa variable también contiene el género que ha introducido.</span><span class="sxs-lookup"><span data-stu-id="f3b67-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="f3b67-333">Pero el `Request` uso del `value` objeto para establecer el atributo como se muestra aquí es la forma estándar de realizar esta tarea.</span><span class="sxs-lookup"><span data-stu-id="f3b67-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="f3b67-334">(Suponiendo que incluso desea &mdash; hacer esto en algunas situaciones, es posible que desee representar la página *sin* valores en los campos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="f3b67-335">Todo depende de lo que está pasando con su aplicación.)</span><span class="sxs-lookup"><span data-stu-id="f3b67-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="f3b67-336">No puede "recordar" el valor de un cuadro de texto que se usa para las contraseñas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="f3b67-337">Sería un agujero de seguridad para permitir que las personas rellenen un campo de contraseña mediante el uso de código.</span><span class="sxs-lookup"><span data-stu-id="f3b67-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="f3b67-338">Vuelva a ejecutar la página, escriba un género y haga clic en **Buscar género**.</span><span class="sxs-lookup"><span data-stu-id="f3b67-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="f3b67-339">Esta vez no solo verá los resultados de la búsqueda, sino que el cuadro de texto recuerda lo que ingresó la última vez:</span><span class="sxs-lookup"><span data-stu-id="f3b67-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Página que muestra que el cuadro de texto ha 'recordado' la entrada anterior](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="f3b67-341">Búsqueda de cualquier palabra en el título</span><span class="sxs-lookup"><span data-stu-id="f3b67-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="f3b67-342">Ahora puede buscar cualquier género, pero también es posible que desee buscar un título.</span><span class="sxs-lookup"><span data-stu-id="f3b67-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="f3b67-343">Es difícil obtener un título exactamente correcto cuando se busca, por lo que en su lugar se puede buscar una palabra que aparece en cualquier lugar dentro de un título.</span><span class="sxs-lookup"><span data-stu-id="f3b67-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="f3b67-344">Para ello en SQL, `LIKE` utilice el operador y la sintaxis como la siguiente:</span><span class="sxs-lookup"><span data-stu-id="f3b67-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="f3b67-345">Este comando obtiene todas las películas cuyos títulos contienen "aventura".</span><span class="sxs-lookup"><span data-stu-id="f3b67-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="f3b67-346">Cuando se `LIKE` utiliza el operador, `%` se incluye el carácter comodín como parte del término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="f3b67-347">La `LIKE 'adventure%'` búsqueda significa "empezar con 'aventura'".</span><span class="sxs-lookup"><span data-stu-id="f3b67-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="f3b67-348">(Técnicamente, significa "La cadena 'aventura' seguida de cualquier cosa.") Del mismo modo, el término `LIKE '%adventure'` de búsqueda significa "cualquier cosa seguida de la cadena 'aventura'", que es otra manera de decir "terminar con 'aventura'".</span><span class="sxs-lookup"><span data-stu-id="f3b67-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="f3b67-349">Por lo `LIKE '%adventure%'` tanto, el término de búsqueda significa "con 'aventura' en cualquier parte del título".</span><span class="sxs-lookup"><span data-stu-id="f3b67-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="f3b67-350">(Técnicamente, "cualquier cosa en el título, seguido de 'aventura', seguido de cualquier cosa.")</span><span class="sxs-lookup"><span data-stu-id="f3b67-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="f3b67-351">Dentro `<form>` del elemento, agregue el siguiente `</div>` marcado justo debajo de `</form>` la etiqueta de cierre para la búsqueda de género (justo antes del elemento de cierre):</span><span class="sxs-lookup"><span data-stu-id="f3b67-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="f3b67-352">El código para controlar esta búsqueda es similar al código para la `LIKE` búsqueda de género, excepto que tiene que ensamblar la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="f3b67-353">Dentro del bloque de código en la `if` parte superior `if` de la página, agregue este bloque justo después del bloque para la búsqueda de género:</span><span class="sxs-lookup"><span data-stu-id="f3b67-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="f3b67-354">Este código utiliza la misma lógica que vio anteriormente, excepto que la búsqueda utiliza un `LIKE` operador y el código coloca "`%`" antes y después del término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="f3b67-355">Observe cómo era fácil agregar otra búsqueda a la página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="f3b67-356">Todo lo que tenías que hacer era:</span><span class="sxs-lookup"><span data-stu-id="f3b67-356">All you had to do was:</span></span>

- <span data-ttu-id="f3b67-357">Cree `if` un bloque que haya probado para ver si el cuadro de búsqueda correspondiente tenía un valor.</span><span class="sxs-lookup"><span data-stu-id="f3b67-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="f3b67-358">Establezca `selectCommand` la variable en una nueva instrucción SQL.</span><span class="sxs-lookup"><span data-stu-id="f3b67-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="f3b67-359">Establezca `searchTerm` la variable en el valor que se va a pasar a la consulta.</span><span class="sxs-lookup"><span data-stu-id="f3b67-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="f3b67-360">Aquí está el bloque de código completo, que contiene la nueva lógica para una búsqueda de título:</span><span class="sxs-lookup"><span data-stu-id="f3b67-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="f3b67-361">Este es un resumen de lo que hace este código:</span><span class="sxs-lookup"><span data-stu-id="f3b67-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="f3b67-362">Las `searchTerm` variables `selectCommand` y se inicializan en la parte superior.</span><span class="sxs-lookup"><span data-stu-id="f3b67-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="f3b67-363">Va a establecer estas variables en el término de búsqueda adecuado (si existe) y el comando SQL adecuado en función de lo que hace el usuario en la página.</span><span class="sxs-lookup"><span data-stu-id="f3b67-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="f3b67-364">La búsqueda predeterminada es el caso simple de obtener todas las películas de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="f3b67-365">En las `searchGenre` pruebas `searchTitle`para y `searchTerm` , el código se establece en el valor que desea buscar.</span><span class="sxs-lookup"><span data-stu-id="f3b67-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="f3b67-366">Esos bloques `selectCommand` de código también se establecen en un comando SQL adecuado para esa búsqueda.</span><span class="sxs-lookup"><span data-stu-id="f3b67-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="f3b67-367">El `db.Query` método se invoca una sola vez, utilizando cualquier comando SQL en `selectedCommand` y cualquier valor en `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="f3b67-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="f3b67-368">Si no hay ningún término de búsqueda (sin `searchTerm` género y sin palabra de título), el valor de es una cadena vacía.</span><span class="sxs-lookup"><span data-stu-id="f3b67-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="f3b67-369">Sin embargo, eso no importa, porque en ese caso la consulta no requiere un parámetro.</span><span class="sxs-lookup"><span data-stu-id="f3b67-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="f3b67-370">Probar la función de búsqueda de títulos</span><span class="sxs-lookup"><span data-stu-id="f3b67-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="f3b67-371">Ahora puede probar su página de búsqueda completada.</span><span class="sxs-lookup"><span data-stu-id="f3b67-371">Now you can test your completed search page.</span></span> <span data-ttu-id="f3b67-372">Ejecute *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f3b67-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="f3b67-373">Introduzca un género y haga clic en **Buscar género**.</span><span class="sxs-lookup"><span data-stu-id="f3b67-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="f3b67-374">La cuadrícula muestra películas de ese género, como antes.</span><span class="sxs-lookup"><span data-stu-id="f3b67-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="f3b67-375">Introduzca una palabra de título y haga clic en **Buscar título**.</span><span class="sxs-lookup"><span data-stu-id="f3b67-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="f3b67-376">La cuadrícula muestra las películas que tienen esa palabra en el título.</span><span class="sxs-lookup"><span data-stu-id="f3b67-376">The grid displays movies that have that word in the title.</span></span>

![Lista de páginas de películas después de buscar 'The' en el título](form-basics/_static/image6.png)

<span data-ttu-id="f3b67-378">Deje ambos cuadros de texto en blanco y haga clic en cualquiera de los botones.</span><span class="sxs-lookup"><span data-stu-id="f3b67-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="f3b67-379">La cuadrícula muestra todas las películas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="f3b67-380">Combinación de las consultas</span><span class="sxs-lookup"><span data-stu-id="f3b67-380">Combining the Queries</span></span>

<span data-ttu-id="f3b67-381">Es posible que observe que las búsquedas que puede realizar son exclusivas.</span><span class="sxs-lookup"><span data-stu-id="f3b67-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="f3b67-382">No se puede buscar el título y el género al mismo tiempo, incluso si ambos cuadros de búsqueda tienen valores en ellos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="f3b67-383">Por ejemplo, no puede buscar todas las películas de acción cuyo título contenga "Aventura".</span><span class="sxs-lookup"><span data-stu-id="f3b67-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="f3b67-384">(Como la página está codificada ahora, si introduce valores tanto para el género como para el título, la búsqueda de título obtiene prioridad.) Para crear una búsqueda que combine las condiciones, tendría que crear una consulta SQL que tenga una sintaxis como la siguiente:</span><span class="sxs-lookup"><span data-stu-id="f3b67-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="f3b67-385">Y tendría que ejecutar la consulta mediante una instrucción como la siguiente (en términos generales):</span><span class="sxs-lookup"><span data-stu-id="f3b67-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="f3b67-386">La creación de lógica para permitir muchas permutaciones de criterios de búsqueda puede implicarse un poco, como puede ver.</span><span class="sxs-lookup"><span data-stu-id="f3b67-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="f3b67-387">Por lo tanto, nos detendremos aquí.</span><span class="sxs-lookup"><span data-stu-id="f3b67-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="f3b67-388">Próximamente</span><span class="sxs-lookup"><span data-stu-id="f3b67-388">Coming Up Next</span></span>

<span data-ttu-id="f3b67-389">En el siguiente tutorial, creará una página que usa un formulario para permitir a los usuarios agregar películas a la base de datos.</span><span class="sxs-lookup"><span data-stu-id="f3b67-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="f3b67-390">Listado completo para la página de la película (actualizado con la búsqueda)</span><span class="sxs-lookup"><span data-stu-id="f3b67-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="f3b67-391">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="f3b67-391">Additional Resources</span></span>

- [<span data-ttu-id="f3b67-392">Introducción a la programación web de ASP.NET usando la sintaxis Razor</span><span class="sxs-lookup"><span data-stu-id="f3b67-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="f3b67-393">[Cláusula SQL WHERE](http://www.w3schools.com/sql/sql_where.asp) en el sitio de W3Schools</span><span class="sxs-lookup"><span data-stu-id="f3b67-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="f3b67-394">[Artículo Definiciones](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) de métodos en el sitio W3C</span><span class="sxs-lookup"><span data-stu-id="f3b67-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f3b67-395">[Anterior](displaying-data.md)
> [Siguiente](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="f3b67-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
